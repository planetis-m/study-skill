# study-skill repo

This repository hosts the installable skill `study-assistant`.

## Install with `$skill-installer`

Use:

```text
$skill-installer install the skill from repo planetis-m/study-skill with path study-assistant
```

Then restart Codex.

## Repo Layout

- `study-assistant/`: the actual skill directory (`SKILL.md`, `agents/`, `references/`)
